# Database is optional - app works without it
# If you have a PostgreSQL database, set DATABASE_URL here
# Example: DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# Leaving it empty means the app runs in offline/localStorage mode
DATABASE_URL=
